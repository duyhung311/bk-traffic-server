const TrafficStatus = require('./traffic-status.service');
const AdminTrafficStatusConfig = require('./admin-traffic-status-config.service');

module.exports = {
  TrafficStatus,
  AdminTrafficStatusConfig,
};
