const Segment = require('./segment');
const SegmentReport = require('./segment-report');
const SpeechReport = require('./speech-report');
const SpeechRecord = require('./speech-record');
const SpeechReportScript = require('./speech-report-script');

module.exports = {
  Segment,
  SegmentReport,
  SpeechReport,
  SpeechRecord,
  SpeechReportScript,
};
