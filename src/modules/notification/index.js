const Service = require('./notification.service');
const Model = require('./notification.model');
const Router = require('./notification.router');

module.exports = {
  Service,
  Model,
  Router,
};
